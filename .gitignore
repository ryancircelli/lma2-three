# Secrets - never commit. Cloudflare credentials live in GitHub Actions secrets.
.env
.env.*
.dev.vars
*.pem

# assets/ IS committed: this repo is private, and the deploy pipeline serves it.
# (Regenerable from an install with `deno task extract`.)


# Build output
/dist/
/_site/

# Screenshots from `deno task shot`
/screenshots/

# Parallel agent worktrees
/.worktrees/

# Screensaver build (screensaver/): the embedded site zip and build output
/screensaver/site.zip
/screensaver/bin/
/screensaver/obj/
